<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>BCA Admission Application</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <style>
    body {
      font-size: 13px;
      line-height: 1.25;
    }

    @media print {
      .no-print {
        display: none;
      }

      body {
        margin: 0;
      }

      .page {
        margin: 0;
        padding: 10mm;
      }
    }
  </style>
</head>

<body class="bg-white text-gray-900">
  <div class="page max-w-[210mm] mx-auto p-6 border shadow bg-white">
    <!-- Header -->
    <div class="flex justify-between items-center border-b pb-3 mb-4">
      <div class="flex items-center gap-3">
        <img src="http://library-bangaloreuniversity.in/Conference_DLL_4/images/3.jpg" alt="College Logo"
          class="w-16 h-16 object-contain">
        <div>
          <h1 class="text-xl font-bold text-indigo-800">BANGALORE UNIVERSITY</h1>
          <p class="text-xs text-gray-600">BCA Admission Application 2025–26</p>
        </div>
      </div>
      <div class="w-24 h-28 border text-[10px] flex justify-center items-center text-center leading-tight">
        Paste<br>Photo<br>Here
      </div>
    </div>

    <!-- Application Info -->
    <div class="grid grid-cols-2 gap-2 mb-3">
      <div><strong>Application No:</strong> <%= data.applicationNumber %></div>
      <div><strong>Submission Date:</strong> <%= data.submittedAt %></div>
    </div>

    <!-- Personal Details -->
    <div class="mb-3">
      <h2 class="text-base font-semibold text-indigo-700 border-b mb-1">Personal Details</h2>
      <div class="grid grid-cols-2 gap-2">
        <div><strong>Name:</strong> <%= data.name %></div>
        <div><strong>DOB:</strong> <%= data.dob %></div>
        <div><strong>Gender:</strong> <%= data.gender %></div>
        <div><strong>Mother:</strong> <%= data.motherName %></div>
        <div><strong>Father:</strong> <%= data.fatherName %></div>
        <div><strong>Mobile:</strong> <%= data.mobile %></div>
        <div><strong>Email:</strong> <%= data.email %></div>
        <div><strong>Nationality:</strong> <%= data.nationality %></div>
        <div><strong>Religion:</strong> <%= data.religion %></div>
      </div>
    </div>

    <!-- Address -->
    <div class="mb-3">
      <h2 class="text-base font-semibold text-indigo-700 border-b mb-1">Address Details</h2>
      <div class="grid grid-cols-2 gap-2">
        <div><strong>Permanent:</strong> <%= data.permVillage %>, <%= data.permTaluka %>, <%= data.permDistrict %> - <%= data.permPincode %>, <%= data.permState %></div>
        <div><strong>Present:</strong> <%= data.presVillage %>, <%= data.presTaluka %>, <%= data.presDistrict %> - <%= data.presPincode %>, <%= data.presState %></div>
      </div>
    </div>

    <!-- Category -->
    <div class="mb-3">
      <h2 class="text-base font-semibold text-indigo-700 border-b mb-1">Category & Certificates</h2>
      <div class="grid grid-cols-2 gap-2">
        <div><strong>Category:</strong> <%= data.category %></div>
        <div><strong>Caste Cert No:</strong> <%= data.casteCertNo %></div>
        <div><strong>Income (Annual):</strong> ₹<%= data.annualIncome %></div>
        <div><strong>Income Cert No:</strong> <%= data.incomeCertNo %></div>
      </div>
    </div>

    <!-- Education -->
    <div class="mb-3">
      <h2 class="text-base font-semibold text-indigo-700 border-b mb-1">Educational Background</h2>
      <div class="grid grid-cols-1 gap-1">
        <div><strong>10<sup>th</sup> Board:</strong> <%= data.tenthBoard %> | <strong>%:</strong> <%= data.tenthPercent %></div>
        <div><strong>12<sup>th</sup> Board:</strong> <%= data.twelfthBoard %> | <strong>Combo:</strong> <%= data.twelfthCombo %></div>
        <div><strong>12<sup>th</sup> Marks:</strong> <%= data.twelfthMarksScored %>/<%= data.twelfthMaxMarks %> | <strong>%:</strong> <%= data.twelfthPercent %></div>
      </div>
    </div>

    <!-- Declaration -->
    <div class="mt-6 border-t pt-3">
      <p class="italic text-[12px]">
        I hereby declare that the above information is true. If found false, I understand my admission is liable to be cancelled.
      </p>
      <div class="flex justify-between mt-6 text-[13px]">
        <div>
          <p>Signature of Parent</p>
          <p class="mt-4">_____________________</p>
        </div>
        <div>
          <p>Signature of Candidate</p>
          <p class="mt-4">_____________________</p>
        </div>
      </div>
    </div>

    <!-- Print Button -->
    <div class="no-print text-center mt-4">
      <button onclick="window.print()" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 text-sm">
        🖨️ Print Application
      </button>
    </div>
  </div>
</body>

</html>
