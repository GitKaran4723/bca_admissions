<!-- views/form.ejs -->
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BCA Admission Form</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
  <script src="https://unpkg.com/lucide@latest"></script>
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
  </style>
</head>

<body class="bg-gradient-to-br from-blue-50 to-blue-100 min-h-screen text-gray-800">
  <div class="max-w-5xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
    <header class="mb-8 text-center">

      <h1 class="text-3xl sm:text-4xl font-extrabold text-indigo-700 mb-1">BCA Admission Form – 2025-26</h1>
      <p class="text-sm sm:text-base text-gray-600">Please fill all fields carefully. Make sure all the information is
        correct.</p>
      <p class="text-sm text-red-600 mb-4 text-center">
        <span class="font-semibold">*</span> Indicates required fields. Please ensure these fields are filled.
      </p>
    </header>

    <form action="/submit" method="POST" class="bg-white p-6 sm:p-8 rounded-2xl shadow-xl space-y-6">
      <div>
        <label class="block text-sm font-semibold mb-1">
          1. Name of the Candidate<span class="text-red-600">*</span>
          <span class="text-xs text-gray-500 block">(Enter exactly as in your 10<sup>th</sup> marks card)</span>
        </label>
        <input name="name" required placeholder="e.g., Ananya R Sharma" class="w-full px-4 py-3 border rounded-xl">
      </div>


      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <div>
          <label class="block text-sm font-semibold mb-1">
            2. Mother’s Name<span class="text-red-600">*</span>
            <span class="text-xs text-gray-500 block">(Enter exactly as in your 10<sup>th</sup> marks card)</span>
          </label>
          <input name="motherName" required placeholder="e.g., Meena R Sharma"
            class="w-full px-4 py-3 border rounded-xl">
        </div>

        <div>
          <label class="block text-sm font-semibold mb-1">
            3. Father’s Name<span class="text-red-600">*</span>
            <span class="text-xs text-gray-500 block">(Enter exactly as in your 10<sup>th</sup> marks card)</span>
          </label>
          <input name="fatherName" required placeholder="e.g., Ramesh S Sharma"
            class="w-full px-4 py-3 border rounded-xl">
        </div>

      </div>

      <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
        <div>
          <label class="block text-sm font-semibold mb-1">
            4. Date of Birth<span class="text-red-600">*</span>
            <span class="text-xs text-gray-500 block">(Enter exactly as in your 10<sup>th</sup> marks card)</span>
          </label>
          <input type="date" name="dob" required min="1995-01-01" max="2010-12-31"
            class="w-full px-4 py-3 border rounded-xl">
        </div>


        <div>
          <label class="block text-sm font-semibold mb-1">5. Gender<span class="text-red-600">*</span></label>
          <select name="gender" required class="w-full px-4 py-3 border rounded-xl">
            <option value="">Select</option>
            <option>Male</option>
            <option>Female</option>
            <option>Other</option>
          </select>
        </div>
        <div>
          <label class="block text-sm font-semibold mb-1">
            6. Student's Mobile Number
            <span class="text-xs text-gray-500 block">(Please enter your phone number if available)</span>
          </label>
          <input type="tel" name="mobile" pattern="^\d{10}$" maxlength="10" placeholder="e.g., 9876543210"
            title="Please enter a valid 10-digit mobile number" class="w-full px-4 py-3 border rounded-xl">
        </div>

        <div class="mt-4">
          <label class="block text-sm font-semibold mb-1">
            Guardian’s Mobile Number<span class="text-red-600">*</span>
            <span class="text-xs text-gray-500 block">(Required for communication)</span>
          </label>
          <input type="tel" name="guardianMobile" required pattern="^\d{10}$" maxlength="10"
            placeholder="e.g., 9876543210" title="Please enter a valid 10-digit guardian mobile number"
            class="w-full px-4 py-3 border rounded-xl">
        </div>
        <script>
          document.querySelectorAll('input[type="tel"]').forEach(input => {
            input.addEventListener('input', function () {
              this.value = this.value.replace(/\D/g, '');
            });
          });
        </script>


      </div>

      <div>
        <label class="block text-sm font-semibold mb-1">
          7. Student’s Email ID<span class="text-red-600">*</span>
          <span class="text-xs text-gray-500 block">(Please enter your email ID)</span>
        </label>
        <input type="email" name="email" required placeholder="e.g., student@example.com"
          class="w-full px-4 py-3 border rounded-xl">
      </div>

      <div class="mt-4">
        <label class="block text-sm font-semibold mb-1">
          Parent’s Email ID
          <span class="text-xs text-gray-500 block">(Optional – for communication purpose)</span>
        </label>
        <input type="email" name="parentEmail" placeholder="e.g., parent@example.com"
          class="w-full px-4 py-3 border rounded-xl">
      </div>

      <div class="mt-4">
        <label class="block text-sm font-semibold mb-1">
          Aadhaar Number <span class="text-red-600">*</span>
          <span class="text-xs text-gray-500 block">(Enter your 12-digit Aadhaar number)</span>
        </label>
        <input type="text" name="adharNumber" required pattern="^\d{12}$" maxlength="12"
          placeholder="e.g., 123412341234"
          title="Enter a valid 12-digit Aadhaar number"
          class="w-full px-4 py-3 border rounded-xl">
      </div>

      <script>
        document.querySelector('input[name="adharNumber"]').addEventListener('input', function () {
          this.value = this.value.replace(/\D/g, '');
        });
      </script>
      


      <!-- Permanent Address -->
      <div class="mb-6">
        <label class="block text-sm font-semibold mb-2">8. Permanent Address<span class="text-red-600">*</span></label>

        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
          <input name="permLandmark" placeholder="Landmark" required class="px-4 py-3 border rounded-xl">
          <input name="permVillage" placeholder="Village" required class="px-4 py-3 border rounded-xl">
          <input name="permPincode" placeholder="Pincode" required pattern="^\d{6}$" maxlength="6"
            class="px-4 py-3 border rounded-xl">
          <input name="permTaluka" placeholder="Taluka" required class="px-4 py-3 border rounded-xl">
          <input name="permDistrict" placeholder="District" required class="px-4 py-3 border rounded-xl">
          <input name="permState" placeholder="State" required class="px-4 py-3 border rounded-xl">
        </div>
      </div>

      <!-- Checkbox to copy address -->
      <div class="flex items-center mb-4">
        <input type="checkbox" id="copyAddress"
          class="mr-2 w-5 h-5 text-blue-600 border-gray-300 rounded focus:ring-blue-500">
        <label for="copyAddress" class="text-sm text-gray-700">Present Address is same as Permanent Address</label>
      </div>

      <!-- Present Address -->
      <div class="mb-6">
        <label class="block text-sm font-semibold mb-2">9. Present Address<span class="text-red-600">*</span></label>

        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
          <input name="presLandmark" id="presLandmark" placeholder="Landmark" required
            class="px-4 py-3 border rounded-xl">
          <input name="presVillage" id="presVillage" placeholder="Village" required class="px-4 py-3 border rounded-xl">
          <input name="presPincode" id="presPincode" placeholder="Pincode" required pattern="^\d{6}$" maxlength="6"
            class="px-4 py-3 border rounded-xl">
          <input name="presTaluka" id="presTaluka" placeholder="Taluka" required class="px-4 py-3 border rounded-xl">
          <input name="presDistrict" id="presDistrict" placeholder="District" required
            class="px-4 py-3 border rounded-xl">
          <input name="presState" id="presState" placeholder="State" required class="px-4 py-3 border rounded-xl">
        </div>
      </div>
      <script>
        document.getElementById('copyAddress').addEventListener('change', function () {
          const fields = ['Landmark', 'Village', 'Pincode', 'Taluka', 'District', 'State'];

          fields.forEach(field => {
            const perm = document.querySelector(`[name="perm${field}"]`);
            const pres = document.querySelector(`[name="pres${field}"]`);

            if (this.checked) {
              pres.value = perm.value;
              pres.readOnly = true;
              pres.classList.add('bg-gray-100');
            } else {
              pres.readOnly = false;
              pres.classList.remove('bg-gray-100');
            }
          });
        });
      </script>


      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <div>
          <label class="block text-sm font-semibold mb-1">9. Nationality<span class="text-red-600">*</span></label>

          <select name="nationality" id="nationality" required class="w-full px-4 py-3 border rounded-xl">
            <option value="">-- Select Nationality --</option>
            <option value="Indian">Indian</option>
            <option value="Other">Other</option>
          </select>

          <!-- Hidden text input for 'Other' nationality -->
          <input type="text" name="otherNationality" id="otherNationality" placeholder="Please specify nationality"
            class="w-full px-4 py-3 border rounded-xl mt-2 hidden">
        </div>

        <script>
          const nationalitySelect = document.getElementById('nationality');
          const otherNationalityInput = document.getElementById('otherNationality');

          nationalitySelect.addEventListener('change', function () {
            if (this.value === 'Other') {
              otherNationalityInput.classList.remove('hidden');
              otherNationalityInput.required = true;
            } else {
              otherNationalityInput.classList.add('hidden');
              otherNationalityInput.required = false;
            }
          });
        </script>

        <div>
          <label class="block text-sm font-semibold mb-1">10. Religion</label>
          <select name="religion" required class="w-full px-4 py-3 border rounded-xl">
            <option value="">-- Select Religion --</option>
            <option value="Hindu">Hindu</option>
            <option value="Muslim">Muslim</option>
            <option value="Christian">Christian</option>
            <option value="Sikh">Sikh</option>
            <option value="Buddhist">Buddhist</option>
            <option value="Jain">Jain</option>
            <option value="Parsi">Parsi</option>
            <option value="Jewish">Jewish</option>
            <option value="Other">Other</option>
          </select>
        </div>
        <input type="text" name="otherReligion" id="otherReligion" placeholder="Please specify religion"
          class="w-full px-4 py-3 border rounded-xl mt-2 hidden">
        <script>
          const religionSelect = document.querySelector('select[name="religion"]');
          const otherReligionInput = document.getElementById('otherReligion');

          religionSelect.addEventListener('change', function () {
            if (this.value === 'Other') {
              otherReligionInput.classList.remove('hidden');
              otherReligionInput.required = true;
            } else {
              otherReligionInput.classList.add('hidden');
              otherReligionInput.required = false;
            }
          });
        </script>

      </div>

      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <div>
          <label class="block text-sm font-semibold mb-1">
            11. Category <span class="text-red-600">*</span>
          </label>
          <select name="category" id="categorySelect" required class="w-full px-4 py-3 border rounded-xl">
            <option value="">Select Category</option>
            <option>General</option>
            <option>SC</option>
            <option>ST</option>
            <option>Cat-1</option>
            <option>2A</option>
            <option>2B</option>
            <option>3A</option>
            <option>3B</option>
          </select>
        </div>

        <div>
          <label class="block text-sm font-semibold mb-1">
            12. Caste Certificate Number <span id="casteCertStar" class="text-red-600 hidden">*</span>
          </label>
          <input name="casteCertNo" id="casteCertNo" pattern="^RD\d{6,15}$" placeholder="e.g., RD123456789012345"
            title="Caste Certificate number must start with 'RD' followed by 6 to 15 digits"
            class="w-full px-4 py-3 border rounded-xl">
        </div>
        <script>
          const categorySelect = document.getElementById("categorySelect");
          const casteCertInput = document.getElementById("casteCertNo");
          const casteCertStar = document.getElementById("casteCertStar");

          categorySelect.addEventListener("change", function () {
            if (this.value === "General") {
              casteCertInput.required = false;
              casteCertInput.value = "";
              casteCertStar.classList.add("hidden");
            } else if (this.value !== "") {
              casteCertInput.required = true;
              casteCertStar.classList.remove("hidden");
            }
          });
        </script>



      </div>

      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <div>
          <label class="block text-sm font-semibold mb-1">13. Parent’s Annual Income <span class="text-red-600">*</span> </label>
          <input type="number" name="annualIncome" required min="0" placeholder="Enter income in ₹ (numbers only)"
            class="w-full px-4 py-3 border rounded-xl">
        </div>

        <div>
          <label class="block text-sm font-semibold mb-1">14. Income Certificate Number <span class="text-red-600">*</span> </label>
          <input name="incomeCertNo" required pattern="^RD\d{6,15}$" placeholder="e.g., RD123456789012345"
            title="Must start with 'RD' followed by 6 to 15 digits" class="w-full px-4 py-3 border rounded-xl">
        </div>

        <script>
          const incomeCertInput = document.querySelector('input[name="incomeCertNo"]');
          incomeCertInput.addEventListener('input', function () {
            this.value = this.value.toUpperCase().replace(/[^A-Z0-9]/g, '');
          });
        </script>

      </div>

      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <div>
          <label class="block text-sm font-semibold mb-1">15. 10<sup>th</sup> Standard Board <span class="text-red-600">*</span></label>
          <select name="tenthBoard" id="tenthBoard" required class="w-full px-4 py-3 border rounded-xl">
            <option value="">Select Board</option>
            <option value="State">State</option>
            <option value="CBSE">CBSE</option>
            <option value="ICSE">ICSE</option>
            <option value="Other">Other</option>
          </select>

          <!-- Hidden input shown only if "Other" is selected -->
          <input type="text" name="otherTenthBoard" id="otherTenthBoard" placeholder="Please specify your board"
            class="w-full px-4 py-3 border rounded-xl mt-2 hidden">
        </div>
        <script>
          const tenthBoardSelect = document.getElementById('tenthBoard');
          const otherTenthInput = document.getElementById('otherTenthBoard');

          tenthBoardSelect.addEventListener('change', function () {
            if (this.value === 'Other') {
              otherTenthInput.classList.remove('hidden');
              otherTenthInput.required = true;
            } else {
              otherTenthInput.classList.add('hidden');
              otherTenthInput.required = false;
            }
          });
        </script>

        <div>
          <label class="block text-sm font-semibold mb-1">16. 10<sup>th</sup> Standard Percentage <span class="text-red-600">*</span></label>
          <input type="number" name="tenthPercent" required step="0.01" min="0" max="100" placeholder="e.g., 89.50"
            title="Enter a valid percentage between 0 and 100" class="w-full px-4 py-3 border rounded-xl">
        </div>

      </div>

      <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
        <div>
          <label class="block text-sm font-semibold mb-1">17. 12<sup>th</sup> Standard Board <span class="text-red-600">*</span></label>
          <select name="twelfthBoard" id="twelfthBoard" required class="w-full px-4 py-3 border rounded-xl">
            <option value="">Select Board</option>
            <option value="State">State</option>
            <option value="CBSE">CBSE</option>
            <option value="ICSE">ICSE</option>
            <option value="Other">Other</option>
          </select>

          <!-- Hidden input field for "Other" option -->
          <input type="text" name="otherTwelfthBoard" id="otherTwelfthBoard" placeholder="Please specify your board"
            class="w-full px-4 py-3 border rounded-xl mt-2 hidden">
        </div>
        <script>
          const twelfthBoardSelect = document.getElementById('twelfthBoard');
          const otherTwelfthInput = document.getElementById('otherTwelfthBoard');

          twelfthBoardSelect.addEventListener('change', function () {
            if (this.value === 'Other') {
              otherTwelfthInput.classList.remove('hidden');
              otherTwelfthInput.required = true;
            } else {
              otherTwelfthInput.classList.add('hidden');
              otherTwelfthInput.required = false;
            }
          });
        </script>

        <div>
          <label class="block text-sm font-semibold mb-1">18. 12<sup>th</sup> Standard Combination <span class="text-red-600">*</span></label>
          <select name="twelfthCombo" id="twelfthCombo" required class="w-full px-4 py-3 border rounded-xl">
            <option value="">Select Combination</option>
            <option value="PCM">PCM</option>
            <option value="PCMB">PCMB</option>
            <option value="PCMC">PCMC</option>
            <option value="Commerce">Commerce</option>
            <option value="Arts">Arts</option>
            <option value="Other">Other</option>
          </select>

          <!-- Shown only when 'Other' is selected -->
          <input type="text" name="otherTwelfthCombo" id="otherTwelfthCombo"
            placeholder="Please specify your combination" class="w-full px-4 py-3 border rounded-xl mt-2 hidden">
        </div>
        <script>
          const twelfthComboSelect = document.getElementById('twelfthCombo');
          const otherComboInput = document.getElementById('otherTwelfthCombo');

          twelfthComboSelect.addEventListener('change', function () {
            if (this.value === 'Other') {
              otherComboInput.classList.remove('hidden');
              otherComboInput.required = true;
            } else {
              otherComboInput.classList.add('hidden');
              otherComboInput.required = false;
            }
          });
        </script>

      </div>

      <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
        <div>
          <label class="block text-sm font-semibold mb-1">19. Max Marks (12<sup>th</sup>) <span class="text-red-600">*</span></label>
          <input type="number" name="twelfthMaxMarks" id="twelfthMaxMarks" required min="1" placeholder="e.g., 600"
            class="w-full px-4 py-3 border rounded-xl">
        </div>

        <div>
          <label class="block text-sm font-semibold mb-1">20. Marks Scored <span class="text-red-600">*</span></label>
          <input type="number" name="twelfthMarksScored" id="twelfthMarksScored" required min="0"
            placeholder="e.g., 540" class="w-full px-4 py-3 border rounded-xl">
        </div>

        <div>
          <label class="block text-sm font-semibold mb-1">21. Percentage</label>
          <input type="number" name="twelfthPercent" id="twelfthPercent" readonly placeholder="Auto-calculated"
            class="w-full px-4 py-3 border rounded-xl bg-gray-100">
        </div>
        <script>
          const maxMarksInput = document.getElementById('twelfthMaxMarks');
          const scoredMarksInput = document.getElementById('twelfthMarksScored');
          const percentInput = document.getElementById('twelfthPercent');

          function calculatePercentage() {
            const max = parseFloat(maxMarksInput.value);
            const scored = parseFloat(scoredMarksInput.value);

            if (!isNaN(max) && max > 0 && !isNaN(scored) && scored >= 0 && scored <= max) {
              const percentage = (scored / max) * 100;
              percentInput.value = percentage.toFixed(2); // Rounded to 2 decimals
            } else {
              percentInput.value = '';
            }
          }

          maxMarksInput.addEventListener('input', calculatePercentage);
          scoredMarksInput.addEventListener('input', calculatePercentage);
        </script>

      </div>

      <!-- Declaration Section -->
      <div class="text-sm text-gray-700 mt-6 flex items-start gap-2">
        <input type="checkbox" id="declarationCheckbox"
          class="mt-1 w-5 h-5 text-green-600 rounded border-gray-300 focus:ring-green-500">
        <label for="declarationCheckbox" class="select-none">
          I hereby declare that the above information is true to the best of my knowledge and belief. If found false, I
          understand that my admission can be cancelled at any stage.
        </label>
      </div>

      <!-- Submit Button -->
      <div class="text-center mt-8">
        <button type="submit" id="submitButton" disabled
          class="bg-green-400 text-white px-6 py-3 rounded-xl font-semibold transition duration-300 ease-in-out cursor-not-allowed">
          ✅ Submit & Generate Application Number
        </button>
      </div>
      <script>
        const checkbox = document.getElementById('declarationCheckbox');
        const submitBtn = document.getElementById('submitButton');

        checkbox.addEventListener('change', function () {
          if (this.checked) {
            submitBtn.disabled = false;
            submitBtn.classList.remove('bg-green-400', 'cursor-not-allowed');
            submitBtn.classList.add('bg-green-600', 'hover:bg-green-700', 'cursor-pointer');
          } else {
            submitBtn.disabled = true;
            submitBtn.classList.add('bg-green-400', 'cursor-not-allowed');
            submitBtn.classList.remove('bg-green-600', 'hover:bg-green-700', 'cursor-pointer');
          }
        });
      </script>

    </form>
  </div>

  <script>
    document.querySelector('form').addEventListener('submit', function (e) {
      const invalid = this.querySelector(':invalid');
      if (invalid) {
        invalid.scrollIntoView({ behavior: 'smooth', block: 'center' });
      }
    });
    document.querySelector('form').addEventListener('submit', function () {
      this.querySelectorAll('input[type="text"], input[type="email"]').forEach(input => {
        input.value = input.value.trim();
      });
    });

    document.querySelector('form').addEventListener('submit', function (e) {
      const confirmed = confirm("⚠️ Once submitted, no changes will be allowed. Do you want to proceed?");
      if (!confirmed) {
        e.preventDefault(); // stop the form from submitting
      }
    });
  </script>

  <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
  <script>AOS.init();</script>
</body>

</html>